# دليل البدء السريع - Quick Start Guide

## 🚀 البدء السريع (5 دقائق)

### 1. تثبيت الحزم
```bash
npm install
```

### 2. تشغيل المشروع
```bash
npm run dev
```

### 3. فتح المتصفح
```
http://localhost:5173/mallaa/
```

### 4. تسجيل الدخول
- **أخصائي**: `specialist_muscat` / `123456`
- **مشرف**: `supervisor` / `123456`

## 📦 النشر على GitHub Pages

### الخطوة 1: تثبيت gh-pages
```bash
npm install --save-dev gh-pages
```

### الخطوة 2: رفع الكود إلى GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/mallaa.git
git push -u origin main
```

### الخطوة 3: النشر
```bash
npm run deploy
```

### الخطوة 4: تفعيل GitHub Pages
1. اذهب إلى Settings → Pages
2. اختر فرع `gh-pages`
3. احفظ

## ✅ التحقق من الإعداد

### قبل النشر، تأكد من:

- [ ] `vite.config.js` يحتوي على `base: '/mallaa/'`
- [ ] `src/main.jsx` يحتوي على `basename="/mallaa"`
- [ ] `package.json` يحتوي على سكريبت `deploy`
- [ ] تم تثبيت `gh-pages`

## 📚 الوثائق الكاملة

- **التثبيت التفصيلي**: راجع `INSTALLATION.md`
- **النشر التفصيلي**: راجع `DEPLOYMENT.md`
- **الوثائق العامة**: راجع `README.md`

## 🆘 مشاكل شائعة

### الصفحة فارغة؟
→ تأكد من إضافة `/mallaa/` في نهاية الرابط

### البيانات لا تظهر؟
→ احذف localStorage وأعد تحميل الصفحة

### النشر لا يعمل؟
→ تأكد من تثبيت `gh-pages` وتفعيل GitHub Pages في الإعدادات
