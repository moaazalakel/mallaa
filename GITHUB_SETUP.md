# دليل إعداد GitHub - GitHub Setup Guide

## 📋 الخطوات الكاملة للنشر على GitHub

### الخطوة 1: إنشاء مستودع على GitHub

1. اذهب إلى [GitHub](https://github.com)
2. اضغط **New repository**
3. أدخل:
   - **Repository name**: `mallaa` (يجب أن يطابق `base` في vite.config.js)
   - **Description**: منصة الملاءة - منظومة قياس نضج خدمات التشخيص التربوي
   - **Visibility**: Public أو Private (حسب الحاجة)
   - **لا** تضع علامة على "Initialize with README"
4. اضغط **Create repository**

### الخطوة 2: إعداد Git محلياً

```bash
# تأكد من أنك في مجلد المشروع
cd /home/moaaz/cursor/inas/mallaa

# تهيئة Git (إذا لم يكن مهيأ)
git init

# إضافة جميع الملفات
git add .

# عمل commit أولي
git commit -m "Initial commit: منصة الملاءة - جاهزة للنشر"
```

### الخطوة 3: ربط المستودع المحلي بـ GitHub

```bash
# استبدل YOUR_USERNAME باسم المستخدم الخاص بك
git remote add origin https://github.com/YOUR_USERNAME/mallaa.git

# تغيير اسم الفرع إلى main
git branch -M main

# رفع الكود
git push -u origin main
```

**ملاحظة**: إذا طُلب منك اسم المستخدم وكلمة المرور:
- استخدم **Personal Access Token** بدلاً من كلمة المرور
- لإنشاء Token: Settings → Developer settings → Personal access tokens → Generate new token

### الخطوة 4: تثبيت gh-pages

```bash
npm install --save-dev gh-pages
```

### الخطوة 5: النشر على GitHub Pages

```bash
npm run deploy
```

هذا الأمر سيقوم بـ:
1. بناء المشروع (`npm run build`)
2. إنشاء فرع `gh-pages`
3. رفع مجلد `dist` إلى فرع `gh-pages`

### الخطوة 6: تفعيل GitHub Pages

1. اذهب إلى: `https://github.com/YOUR_USERNAME/mallaa/settings/pages`
2. في قسم **Pages**:
   - **Source**: اختر **Deploy from a branch**
   - **Branch**: اختر `gh-pages`
   - **Folder**: اختر `/ (root)`
3. اضغط **Save**

### الخطوة 7: الانتظار والتحقق

- انتظر 1-2 دقيقة حتى يتم النشر
- افتح: `https://YOUR_USERNAME.github.io/mallaa/`
- تأكد من أن الموقع يعمل بشكل صحيح

## 🔄 تحديث الموقع

عند إجراء أي تغييرات:

```bash
# إضافة التغييرات
git add .

# عمل commit
git commit -m "Update: وصف التحديث"

# رفع التغييرات
git push

# نشر التحديث
npm run deploy
```

## 🔐 إنشاء Personal Access Token

إذا طُلب منك Token:

1. اذهب إلى: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. اضغط **Generate new token (classic)**
3. أدخل:
   - **Note**: "Mallaa Deployment"
   - **Expiration**: اختر المدة المناسبة
   - **Scopes**: حدد `repo` (كامل الوصول للمستودعات)
4. اضغط **Generate token**
5. **انسخ Token فوراً** (لن يظهر مرة أخرى)
6. استخدمه ككلمة مرور عند `git push`

## ✅ التحقق من النشر الناجح

بعد النشر، تحقق من:

- [ ] الموقع يفتح على: `https://YOUR_USERNAME.github.io/mallaa/`
- [ ] الصفحة الرئيسية تظهر بشكل صحيح
- [ ] تسجيل الدخول يعمل
- [ ] جميع الصفحات قابلة للوصول
- [ ] الرسوم البيانية تعمل
- [ ] تصدير PDF يعمل

## 🐛 حل المشاكل الشائعة

### المشكلة: `git push` يطلب مصادقة

**الحل**: استخدم Personal Access Token بدلاً من كلمة المرور

### المشكلة: `npm run deploy` يفشل

**الحلول**:
1. تأكد من تثبيت `gh-pages`: `npm install --save-dev gh-pages`
2. تأكد من أنك في المجلد الصحيح
3. تحقق من أن `package.json` يحتوي على سكريبت `deploy`

### المشكلة: الصفحة تظهر فارغة بعد النشر

**الحلول**:
1. تأكد من المسار: `/mallaa/` في نهاية الرابط
2. تحقق من `base` في `vite.config.js`
3. تحقق من `basename` في `src/main.jsx`
4. افتح Developer Tools وتحقق من الأخطاء

### المشكلة: التوجيه لا يعمل

**الحل**: تأكد من:
- استخدام `BrowserRouter` (موجود)
- إعداد `basename` بشكل صحيح (موجود)

## 📝 ملاحظات مهمة

1. **اسم المستودع**: يجب أن يطابق `base` في `vite.config.js` (حالياً: `mallaa`)
2. **الفرع**: GitHub Pages يستخدم فرع `gh-pages` تلقائياً
3. **التحديثات**: قد تستغرق 1-2 دقيقة حتى تظهر
4. **المسار**: دائماً استخدم `/mallaa/` في نهاية الرابط

## 🎉 النشر الناجح!

بعد إكمال جميع الخطوات، سيكون موقعك متاحاً على:
```
https://YOUR_USERNAME.github.io/mallaa/
```
